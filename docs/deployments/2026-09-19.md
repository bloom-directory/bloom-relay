# Initial production infrastructure — 2026-09-19

Status: core infrastructure active, public enrollment closed. This is not a
claim of completed Triad rollout, real passkey acceptance, or monitored recovery.

## Deployed subject

- Relay binaries: `6847a6a65dd74f6ce3ae439413cbb1e44e4cab25`.
- CI package SHA-256: `7bde7d4fa8e9fcd5a1170ed1ac10274edfdb3e8733a6eb89ed3a5bccd66cf911`.
- Platform: Debian 13 x86_64, PostgreSQL 17, HAProxy 3.0.
- Placement: `relay-1`; distinct control and Browser ingress public IPv4s.
- DNS: delegated Route 53 `relay.bloom.directory` zone, parent on Cloudflare.
- Separate serving and challenge IAM users use systemd credential files.
- The API, gateway and both DNS workers run as separate Unix/database users;
  explicit peer usernames and AWS region override the earlier package examples.
- Public control TLS uses Certbot with an executable deploy hook and timer.
  Browser certificate keys remain with Brokers.

Server-specific IPs, hosted-zone ID, secrets and trusted installation pins are
managed in restricted host configuration. No wallet or passkey is stored here.

## Evidence

Delegated NS and restrictive parent CAA are visible. Both IAM identities can
read the delegated zone; real UPSERT/DELETE diagnostic records proved their
allowed A/TXT writes and cross-role AccessDenied results. Diagnostic records
were removed. A later disposable probe enrolled one test installation; see the
[pause checkpoint](../resume-2026-09-19.md) for its cleanup and current service state.

All four core services report private readiness 200. PostgreSQL peer identities
match each service, and API/gateway cannot read DNS credential source files.
The database has six migrations and the restore witness is group-private outside
the PostgreSQL data directory. Database, backend TLS and diagnostics bind only
to loopback. External control TLS validates and negotiates HTTP/2; both backend
TLS listeners validate with ALPN h2. Public bootstrap routes return 503 through
an explicit HAProxy enrollment gate. Certbot simulated renewal with deploy hooks
passed; all four private readiness endpoints and external TLS remained healthy
after the API/gateway restart and HAProxy reload.

The owner has authorized continued disposable testing without backups. Recovery
requirements and mandatory disaster-recovery drills are tracked in
[bloom-relay #2](https://github.com/bloom-directory/bloom-relay/issues/2); they
remain a production rollout gate.

## Outstanding rollout gates

- Preserve authenticated client source addresses for API bootstrap quotas;
  HAProxy currently makes those clients share its loopback source bucket.
- Configure a reviewed CT feed adapter, alert destination and operational alerts;
  the CT unit is deliberately disabled rather than using placeholder endpoints.
- Choose managed PostgreSQL versus local PostgreSQL, configure independent
  off-server retention of recovery evidence and key material, define acceptable
  data-loss/recovery times, alert on freshness, and rehearse a restore. The local
  witness alone does not protect against losing or rolling back the entire host.
- Complete real DNS/CA/Broker renewal and browser/passkey ceremony acceptance,
  including the previously unresolved macOS installed NFS startup gate.
  The current deployed smoke uses synthetic pinned TLS, not public issuance.
  Broker currently hardcodes the production ACME directory, and relay store/DNS
  validation reject staging account URIs. A reviewed staging configuration and
  opt-in Broker test must reuse its existing `instant-acme` certificate lifecycle
  to prove issuance and forced-due renewal without touching the installed Triad.

The enrollment gate must stay closed until the applicable rollout evidence is
reviewed. No Triad updates were deployed by this infrastructure setup.

## Resumed validation

The resume fixed three concrete failures: strict client response-clock checks,
PostgreSQL NUMERIC/FLOAT8 decoding in challenge jobs and metrics, and gateway
reliance on backend HTTP/2 connection identity. CONNECT now independently
validates the scoped tunnel bearer, installation, hostname and lease generation
before taking its matching single-use ticket. Per-POST lifecycles support
multiplexing, bounded cancellation and monotonic reconnect replacement.

Local validation passed 43 workspace tests, strict workspace Clippy and formatting.
The separately invoked HAProxy fixture passed with both 3.0 and 3.2, including
missing/wrong bearer rejection, foreign-ticket preservation, stale generation,
replay, multiplexed POST reset isolation, opaque TLS and reconnect fencing.
Standalone deployed-smoke checks passed. The [push CI run](https://github.com/bloom-directory/bloom-relay/actions/runs/35453450321)
and [PR CI run](https://github.com/bloom-directory/bloom-relay/actions/runs/35453451538)
passed all gates, including borrower Rust 1.85, dependency policy, fuzz and package
checks. The exact push-run artifact above was verified locally and on the server
before installation; configuration, database, witness and prior release were
preserved. All four core services restarted ready, and the challenge worker
completed all six paused-test jobs without restarting.

The opt-in live probe then passed against the deployed HAProxy 3.0.11:

- signed enrollment and receipt verification;
- separate tunnel and DNS credential issue;
- ACME account registration and authoritative/recursive serving-DNS readiness;
- DNS-01 TXT lease readiness and deletion;
- eight concurrent pinned synthetic TLS streams through public ingress;
- tunnel reconnect and a further successful opaque TLS exchange;
- signed permanent retirement.

Test installation `2cd92135-311f-44a5-80d3-9fd50a5b291d`, hostname
`2nysrfd4shfjvpxxr3e3eqkl5e.relay.bloom.directory`, is retired with its permanent
reservation retained. Explicit-IP TLS to its retired SNI is rejected before
certificate exchange, so cached DNS cannot restore routing. The local probe
exited successfully and released port 18735. Public bootstrap is closed again
(HTTP 503); its temporary operator allowlist and auto-close timer are removed.

This is transport/DNS evidence, not real public certificate issuance or browser,
passkey, Signer or wallet acceptance. Dependent repositories still need repinning
after merge to include the clock fix and required CONNECT authentication headers.
Backups, CT/alerts and the other rollout gates above remain outstanding.

Final cleanup verification: all six outbox jobs completed. All four authoritative
nameservers and the relay's system recursive resolver returned NXDOMAIN with no
A, AAAA, CAA or TXT answers for the retired installation/challenge. Both disposable
installations in the database are retired. All four readiness checks remained
200, the challenge worker restart count remained zero, and the temporary timer
was inactive.
