# Paused relay infrastructure work — 2026-09-19

Historical pause checkpoint; the owner subsequently requested resumption.
Keep this record as evidence of the starting state. See
[deployment evidence](deployments/2026-09-19.md) for subsequent results.
No Triad rollout or release publication has occurred. Infrastructure remains
running, with public enrollment closed. Backups are deliberately absent during
disposable testing; production rollout is blocked by [issue #2](https://github.com/bloom-directory/bloom-relay/issues/2).

## Saved code and evidence

The checkpoint extends PR #1 on `codex/remote-ceremonies`:

- Client/protocol checks now tolerate up to five seconds of positive server clock
  offset in authenticated response timestamps. Expiration and server-side signed
  authorization lifetimes remain strict. Real enrollment failed at only 65–82 ms
  of offset before this fix. Eight focused tests and strict Clippy passed.
- `tools/deployed-smoke` is an opt-in standalone locked Rust workspace, using the
  maintained relay clients and Rust TLS/crypto libraries. It creates a disposable
  allocation, scoped credentials, serving DNS and DNS-01 lease, exercises synthetic
  opaque TLS, and attempts signed retirement on normal failure/success. It does
  not issue a public certificate or prove browser/passkey/Signer behavior.
- An ignored Docker HAProxy 3.2 fixture in the gateway reproduces a real transport
  integration bug: tunnel POST succeeds, but CONNECT returns 410 because HAProxy
  does not preserve the gateway's assumed backend HTTP/2 connection affinity.
  This is intentionally a failing reproducer, not completed coverage.
- Partial client-only CONNECT authentication headers were removed at pause;
  implementing half of the transport change is not the saved baseline.
- The full workspace suite passed before the final diagnostic fixture edits and
  clock fix. The final checkpoint compile/format result is recorded below.

## First work on resume

1. Read contributor instructions, README, linked remote architecture, wire.md,
   operations.md, deployment evidence and issue #2. Inspect git status before edits.
2. Fix the deployed DNS challenge worker crash before restarting it:
   `crates/bloom-relay-store/src/lib.rs::challenge_for_job` selects
   `extract(epoch FROM expires_at)*1000` (PostgreSQL NUMERIC) and decodes it with
   `row.get::<f64>` (FLOAT8), which panics. Use an explicit checked compatible
   SQL/Rust type and a real PostgreSQL regression test, including cleanup jobs.
   The worker was stopped at pause to prevent continued restarts; queued jobs
   and tombstones must be preserved. No new binary was deployed as a quick fix.
3. Resolve the HTTP/2 proxy boundary before another live tunnel test. Do not simply
   replace same-connection authentication with possession of a ticket. Proposed
   direction: each CONNECT supplies scoped tunnel bearer, installation/host/version
   and tunnel generation; validate exact ticket owner, current generation, bearer
   scope and live tunnel. Keep short-lived single-use ticket claims atomic.
4. Handle multiple tunnel POST streams sharing a backend connection. The existing
   per-connection `active: Option` cannot represent multiplexed installations.
   Preferred review direction is supervised per-POST tunnel lifecycle/heartbeats;
   investigate cancellation, reconnect fencing and cleanup. HAProxy connection
   reuse settings alone are not a sufficient authority boundary. No final design
   or implementation is approved as tested yet.
5. Add negative tests for missing/wrong bearer, another installation, stale
   generation, replay, multiple POSTs and separate CONNECT backend connections.
   Update wire contract and run direct plus proxied transport tests. Add the
   standalone smoke checks and appropriate proxy fixture invocation to CI.
6. Rebuild/review the Linux package and update the deployed gateway only after
   checks pass. Current server still runs the older binary listed below.
7. Temporarily allow only the operator source IP for disposable bootstrap, run
   the native live smoke, verify retirement/DNS cleanup, then close enrollment.
8. Continue real CA/Broker renewal and local/remote browser/passkey acceptance.
   Backup/restore drills, CT/alerts and trusted API source-IP quotas remain rollout
   gates. The two-login macOS workflow was explicitly deferred by the owner;
   the single-login installed NFS startup timeout remains unresolved.

## Environment and operational references

- SSH alias `relay`; task known-hosts file `/tmp/bloom-relay-known-hosts`.
- Deployed binaries: `d08e378b6a6149db0243577637307561993b3fd2`.
- Package SHA-256: `ced7ffce9c2c29c1e95005ea67c10f0361c26bf68f30a07c3a44333b0937fcae`.
- Debian 13, PostgreSQL 17, HAProxy 3.0.11; API, gateway and serving worker remain active; challenge worker is stopped (see below).
- Control `relay-control.bloom.directory` / 84.32.151.158, ingress 84.32.25.82.
- Route53 delegated child zone `relay.bloom.directory`: Z027749338WRWB5UYVYRV.
  Parent DNS remains Cloudflare. Separate serving/challenge IAM credentials stay
  root-only on server; never copy or print them.
- Closed enrollment config `/root/bloom-relay-haproxy-production.cfg`; deployed
  `/etc/haproxy/haproxy.cfg`. The operator allowlist copy must not be left active.
- Certbot renewal timer and deploy hook are installed; simulated renewal passed.
- Local restore witness is outside PG data, but on the same host; no independent
  backup/witness retention, CT feed or alert destination has been configured.
- Do not stop the installed local Broker on 18734. Test listener 18735 must be free.
- Disposable local PG container `bloom-relay-test-pg`, port 55438, database relaytest;
  URL `postgres://postgres:relaytest@127.0.0.1:55438/relaytest` is test-only.
- Public smoke inputs in `/tmp/bloom-relay-deployed-smoke`: control-ca.pem,
  receipt-public-key.hex, acme-account-uri. No private signing/ACME key is copied.
  Recreate public inputs if /tmp is cleared; follow tools/deployed-smoke/README.md.
- Logs `/tmp/bloom-relay-live-smoke.log`, `/tmp/bloom-relay-test-current.log`, and
  `/tmp/bloom-relay-pause-check.log` are local evidence, not durable prerequisites.

## PR landing order

1. [runtime #19](https://github.com/bloom-directory/bloom-service-runtime/pull/19)
2. [relay #1](https://github.com/bloom-directory/bloom-relay/pull/1)
3. [Signer #58](https://github.com/bloom-directory/bloom-signer/pull/58)
4. [Broker #73](https://github.com/bloom-directory/bloom-broker/pull/73)
5. [Machine #306](https://github.com/bloom-directory/bloom/pull/306)

Owner will merge and repin dependent repositories in this order. Relay borrower
pin `329b955454db23813e24b0947142c0398bed4b00` predates the clock fix; repin it
and the eventual transport fix before claiming downstream live acceptance.

## Other repository checkpoints

All four other worktrees were clean at pause:

- runtime `a046fd6075853e8591f295983b572c6e1d65d12d`
- Signer `7dddac2985bf00810589f02b4081062ef576a63b`
- Broker `f51fc152a35425f09efd2ecbd60eb92b5b243054`
- Machine `d3424644a402b4507a5d3c23096ebcf978a4c7be`

## Final pause state

- Final `cargo fmt --all -- --check`, `git diff --check` and
  `cargo check -p bloom-relay-client -p bloom-relay --tests --locked` passed.
- Temporary operator bootstrap allowlist removed; external POST challenge is 503.
- DNS challenge worker stopped deliberately; leave it stopped until the SQL decode
  bug is fixed and the reviewed package deployed. Other core services and HAProxy
  remain active. Backups are still absent.
- Live test installation: `922cd087-9183-4e83-9f66-4e4f7841c1d4`, hostname
  `gey2s5msa6qumyltbhshrj2xam.relay.bloom.directory`. Enrollment, scoped credential
  issue, ACME account registration and serving-DNS readiness passed. Challenge
  readiness timed out; tunnel tests were not reached.
- Probe exited normally through error cleanup: signed lease deletion and retirement
  succeeded. Database confirms `retired`, with the permanent reservation retained.
  Port 18735 is free. At the final check, `publish_txt`, `remove_txt`,
  `remove_txt_all` and `remove_records` outbox jobs were still pending; verify DNS
  reconciliation after the worker fix, and do not delete queued work or tombstones.
- No probe or agent work remains running. Infrastructure services remain running
  as described above; this pause does not shut down the provisioned server.
